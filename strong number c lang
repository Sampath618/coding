#include<stdio.h>
#include<conio.h>
void main()
{
int n,str=0,fact,facto;	
scanf("%d",&n);
while(n!=0)
{
		fact=n/10;
		facto=fact*(n-1);   
		n=n%10;
	} 
	 if(facto==n)
		printf("%d is a strong number",n);
	else
		printf("%d is not a strong number",n);
	getch();
} 
